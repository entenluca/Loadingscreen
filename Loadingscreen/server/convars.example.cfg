# Discord / Spielerprofil – diese Zeilen in deine server.cfg kopieren.
# NIEMALS den Bot-Token in config.js oder andere Dateien im Resource-Ordner eintragen!
# Den Token auch nicht ins Git-Repository committen.
#
# WICHTIG: Bot-Token mit "set" (NICHT "setr") – bleibt nur auf dem Server, Clients sehen ihn nicht.
# Debug-Logs in der Server-Konsole: set loadingscreen:debug true
#
# --- Discord-ID fuer Spieler (Pflicht fuer Avatar + @username) ---
# Jeder Spieler muss Discord mit FiveM verknuepfen:
#   1. FiveM oeffnen -> Zahnrad (Einstellungen)
#   2. "Kontoverknuepfungen" / "Linked accounts"
#   3. Discord verbinden und bestaetigen
#   4. FiveM einmal komplett neu starten
#   5. Erneut auf den Server connecten
#
# Ohne discord:-Identifier liefert FiveM keine Discord-Daten – nur der FiveM-Name wird angezeigt.

set loadingscreen:discord_bot_token "HIER_DEIN_DISCORD_BOT_TOKEN"
set loadingscreen:use_lanyard true
